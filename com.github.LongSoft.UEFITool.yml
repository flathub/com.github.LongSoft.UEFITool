app-id: com.github.LongSoft.UEFITool
runtime: org.kde.Platform
runtime-version: '6.3'
sdk: org.kde.Sdk
rename-desktop-file: uefitool.desktop
rename-icon: uefitool
command: uefitool
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: UEFITool
    buildsystem: cmake
    sources:
      - type: git
        url: https://github.com/LongSoft/UEFITool
        tag: A65
        commit: 62d96a16df028fa913f5f9a2ce4b43ba743c5f6a
        x-checker-data:
          type: git
          tag-pattern: (^A\d+$)
          is-main-source: true
  - name: appdata
    buildsystem: simple
    build-commands:
      - mkdir -p /app/share/metainfo
      - mv ${FLATPAK_ID}.appdata.xml /app/share/metainfo/${FLATPAK_ID}.metainfo.xml
    sources:
      - type: file
        path: com.github.LongSoft.UEFITool.appdata.xml